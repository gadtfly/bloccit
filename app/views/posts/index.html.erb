<h1>All Posts</h1>
<% @posts.each do |post| %>
  <div class="media">
    <div class="media-body">
      <h4>
         <p><%= link_to post.title, post %></p> <!--shortcut for: <p><%= link_to post.title, post_path(post.id) %></p> -->
      </h4>
    </div>
  </div>
<% end %>
